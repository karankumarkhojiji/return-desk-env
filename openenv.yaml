spec_version: 1
name: return_desk_env
version: "0.1.0"
description: "Deterministic e-commerce returns and refunds environment for OpenEnv"
type: space
runtime: fastapi
app: server.app:app
port: 8000
action: ReturnDeskAction
observation: ReturnDeskObservation
state: ReturnDeskState
